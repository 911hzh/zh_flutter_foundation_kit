# uikit 目录说明

`uikit` 用于放置多个模块共享的 UI 组件和视觉基础设施。当前目录暂时为空，后续可以用于沉淀通用 Widget、样式封装、弹窗、列表项、按钮等可复用展示组件。

## 适合放置的内容

- 多个模块都会使用的基础 Widget。
- 统一样式的按钮、卡片、列表项、空状态和加载状态组件。
- 与业务无关的 UI 辅助组件。

## 使用约定

`uikit` 中的组件应尽量保持无业务状态或弱业务语义。具体业务流程、接口调用和状态管理仍应留在 `module`、`base` 或 `infra` 对应目录中。
