name: flutter_foundation_kit
description: Flutter 基础库
version: 0.0.2
repository: https://github.com/911hzh/zh_flutter_foundation_kit

environment:
  sdk: ^3.7.0

dependencies:
  async: ^2.13.1
  dio: ^5.9.0
  flutter:
    sdk: flutter
  logger: ^2.4.0
  synchronized: ^3.2.0
  flutter_keychain: ^3.0.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - test/wcore/settings/development.json
    - test/wcore/settings/release.json
